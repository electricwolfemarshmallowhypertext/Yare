# Project State

- Sticky runtime remains the underlying execution substrate.
- Yare v1 focuses on verified work-state memory and auditable receipts.
- Hackathon architecture: CockroachDB is the primary system of record; local `.yare` and `.sticky` outputs are optional fallback/export modes.
- Personas are internal behavioral architecture packs until public exposure is required.
